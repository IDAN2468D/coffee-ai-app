import React from 'react';
import TeamClient from './TeamClient';

export const metadata = {
    title: 'הצוות שלנו | The Digital Roast',
    description: 'הכירו את הצוות המקצועי שמאחורי The Digital Roast - מומחי קפה, AI ועיצוב',
};

export default function TeamPage() {
    return <TeamClient />;
}

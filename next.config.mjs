/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // reactCompiler: true, // Note: reactCompiler might not be supported in Next.js 14.2.0 or requires experimental flag
};

export default nextConfig;
